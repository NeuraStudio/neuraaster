name: NeuraAster Android APK

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          cache: true

      - name: Create local.properties
        run: |
          echo "flutter.sdk=$FLUTTER_ROOT" > android/local.properties

      - name: Install dependencies
        run: flutter pub get

      - name: Patch plugins for compatibility
        run: |
          python3 - <<'PY'
          import glob, os, re

          # 1. Patch permission_handler_android to remove invalid API 36 symbols
          for fpath in glob.glob(os.path.expanduser('~/.pub-cache/hosted/pub.dev/permission_handler_android*/android/src/main/java/com/baseflow/permissionhandler/PermissionUtils.java')):
              with open(fpath, 'r', encoding='utf-8', errors='ignore') as f:
                  txt = f.read()

              txt = txt.replace("case Manifest.permission.ACCESS_LOCAL_NETWORK:", "// case Manifest.permission.ACCESS_LOCAL_NETWORK:")
              txt = re.sub(r'Build\.VERSION\.SDK_INT\s*>=\s*Build\.VERSION_CODES\.CINNAMON_BUN[\s\S]*?permissionNames\.add\(Manifest\.permission\.ACCESS_LOCAL_NETWORK\);', '// removed ACCESS_LOCAL_NETWORK', txt)

              with open(fpath, 'w', encoding='utf-8') as f:
                  f.write(txt)
              print("Patched PermissionUtils.java successfully!")

          # 2. Patch gradle files for AGP and compileSdk 34
          for g in glob.glob(os.path.expanduser('~/.pub-cache/hosted/pub.dev/*/android/build.gradle')):
              with open(g, 'r', encoding='utf-8', errors='ignore') as f:
                  lines = f.readlines()

              new_lines = []
              for line in lines:
                  if "apply plugin:" in line and "kotlin" in line:
                      new_lines.append("// removed kotlin plugin\n")
                  elif "compileSdkVersion" in line:
                      new_lines.append("    compileSdkVersion 34\n")
                  elif "compileSdk " in line and "compileSdkVersion" not in line:
                      new_lines.append("    compileSdk 34\n")
                  else:
                      new_lines.append(line)

              txt = "".join(new_lines)
              txt = re.sub(r"kotlinOptions\s*\{[^}]*\}", "// removed kotlinOptions", txt)

              with open(g, 'w', encoding='utf-8') as f:
                  f.write(txt)
              print("Patched gradle:", g)
          PY

      - name: Build Android APK
        run: flutter build apk --release --android-skip-build-dependency-validation

      - name: Verify APK
        run: |
          ls -lh build/app/outputs/flutter-apk/app-release.apk

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: NeuraAster-Release-APK
          path: build/app/outputs/flutter-apk/app-release.apk
